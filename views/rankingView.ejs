<!DOCTYPE html>
<html>

<head>
    <title>Ranking</title>
    <link rel="stylesheet" href="/style/defaultStyle.css">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>
    <link href="https://fonts.googleapis.com/css?family=Ubuntu" rel="stylesheet">
    <style>
        th, td {
            text-align: center;
            font-size: large;
            padding: 5px;
            background: #FFFFFF;
            border-left: 2px solid rgba(0, 172, 168, 0.3);
            border-bottom: 2px solid rgba(0, 172, 168, 0.3);
        }
        .td_rate {
            border-left: 2px solid rgba(255, 0, 44, 0.2);
            border-bottom: 2px solid rgba(255, 0, 44, 0.2);
        }
        th {
            background-color: rgba(0, 172, 168, 1.0);
            color: #FFFFFF;
        }
        .th_rate {
            background-color: rgba(255, 0, 44, 1.0);
            color: #FFFFFF;
        }
        tr{
            border-radius: 1em;
            overflow: hidden;
        }
        table {
            border-radius: 1em;
            overflow: hidden;
        }
        body {
            background-image: url(../image/background/8.jpg);
            background-size: contain;
            font-family: 'Ubuntu', sans-serif;
        }
        strong {
            font-size: 20px;
            color: #FFFFFF;
            text-align: center;
        }
        caption {
            text-align: center;
        }
    </style>
</head>

<body>
    <%- include('./components/navbar') -%>
        <div>
            <table style="width:50%;float:left;">
                <caption>
                    <strong style="font-size: 20px;color:#FFFFFF;text-align: center;">High Score</strong>
                </caption>
                <% if(histories.length> 10) histories.length = 10; %>
                    <tr>
                        <th>#</th>
                        <th>Username</th>
                        <th>Score</th>
                        <th>Date</th>
                    </tr>
                    <% for (var i=0; i < histories.length; i++) { %>
                        <tr>
                            <td>
                                <%= (i+1) %>
                            </td>
                            <td>
                                <%= histories[i].winner %>
                            </td>
                            <td>
                                <%= histories[i].winnerScore %>
                            </td>
                            <td>
                                <%= histories[i].Date.toLocaleString() %>
                            </td>
                        </tr>
                        <% } %>
            </table>
            <table style="width:50%;float:right;">
                <caption>
                    <strong style="font-size: 20px;color:#FFFFFF;">Winning Rate</strong>
                </caption>
                <% if(users.length> 10) users.length = 10; %>
                    <tr>
                        <th class="th_rate">#</th>
                        <th class="th_rate">Username</th>
                        <th class="th_rate">Winning Rate</th>
                    </tr>
                    <% for (var i=0; i < users.length; i++) { %>
                        <% if (users[i].rate !=0) { %>
                            <tr>
                                <td class="td_rate">
                                    <%= (i+1) %>
                                </td>
                                <td class="td_rate">
                                    <%= users[i].username %>
                                </td>
                                <td class="td_rate">
                                    <%= users[i].rate %>%
                                </td>
                            </tr>
                    <% } %>
                <% } %>
            </table>
        </div>
</body>

</html>