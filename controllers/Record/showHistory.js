const History = require('../../models/History.js')

module.exports = async (req,res)=>{ 
    // await History.create("game1","p1", "p2", 100, 11)
    // History.find({}, (err, history) =>{
    //     console.log(err, history)
    // })
}
